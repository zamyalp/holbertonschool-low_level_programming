#include <stdio.h>
#include "main.h"
/**
 * main - prints its name + \n
 * @argc: number of arguments typed
 * @argc: array pointing to arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (argc);
}
